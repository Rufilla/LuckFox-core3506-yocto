# Build ext4 into the kernel (not as module) so we can mount root
# without an initramfs. The vendor defconfig sets CONFIG_EXT4_FS=m
# because it targets UBIFS/SquashFS on SPI NAND.
CONFIG_EXT4_FS=y
CONFIG_EXT4_USE_FOR_EXT2=y
