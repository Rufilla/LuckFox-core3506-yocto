# Foxbridge dashboard — CGI + static config
server.modules += ("mod_cgi")
cgi.assign = (".cgi" => "")
index-file.names += ("index.html")

# JSON MIME type for the API endpoint
mimetype.assign += (".json" => "application/json")
